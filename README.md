## Getting started

```shell
npm i
```

Open a terminal and run the following command:

```shell
tsx src/server.ts
```

Open another terminal and run the following command:

```shell
tsx src/client.ts
```

